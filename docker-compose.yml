version: "3.8"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        environment:
            - PORT=3000
            - DATABASE_URL=${DATABASE_URL}
            - NODE_ENV=production
        restart: unless-stopped
